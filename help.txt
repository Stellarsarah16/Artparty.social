# ---- Future deployments --------------
# On your server
cd /opt/artparty-social
git pull origin main

# Rebuild and restart
cd deployment/production
docker-compose -f docker-compose.prod.yml build --no-cache
docker-compose -f docker-compose.prod.yml up -d

# No more restart loops or SSL issues! 🎉

--------------------------

get into bash: 
    docker-compose exec backend bash

Navigate To directory:
    cd StellarCollabApp

Start the App:
    docker-compose up --build

# View all logs
    docker-compose logs -f

# View specific service logs
    docker-compose logs -f backend
    docker-compose logs -f frontend

stop the App:
    docker-compose down

Start fresh with clean DB:
    docker-compose down -v

# Test the health endpoint:
    curl http://localhost:8000/health

# Stop all Docker containers
    docker stop $(docker ps -aq)

# Remove all containers (both old and new)
    docker rm $(docker ps -aq)

# Remove all unused images
    docker image prune -a -f

# Remove all unused volumes (optional - this will delete your database data)
    docker volume prune -f

# Remove all unused networks
    docker network prune -f

--- CLEANUP ---------

# backup if needed
    mv your-app-folder your-app-folder-backup

# remove existing folder
    rm -rf your-app-folder



# Restart App
    sudo systemctl restart your-app.service


--- PULL FROM GITHUB------------------

# SSH into bash
    ssh root@your_ip

# Pull new changes
    git pull origin main




    -------------------------------------
DB_PASSWORD  HWIjL5ORT1YYMck
REDIS_PASSWORD  FHfY9WVgoSKcGIRrRGuqVajz5ZR2aG3W
64 bit string
    gPEL0ZEHFztN3VqmPQpZbMjWsyWvIM5erlcJhUkWw6NePPNu3SrnR6oB3BCY7G6O